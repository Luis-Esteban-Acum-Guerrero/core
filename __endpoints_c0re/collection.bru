meta {
  name: API C0RE
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  base_url: http://localhost:3000/api
  access_token: 
  refresh_token: 
}

docs {
  Colección actualizada para la API con estructura de base de datos creceideas_c0re
}
