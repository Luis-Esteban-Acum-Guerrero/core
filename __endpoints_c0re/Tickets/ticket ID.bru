meta {
  name: ticket ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}/ticket/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
}
